# History-router fallback: always serve index.html so deep links
# like /login and /admin load the SPA instead of 404ing.
server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  location / {
    try_files $uri $uri/ /index.html;
  }
}
